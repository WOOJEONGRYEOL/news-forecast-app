# 정상 부팅 확인된 조합으로 고정 (의존성 자동 상승 재발 방지)
# 웹 스택: fec554b 배포에서 부팅 확인된 streamlit 1.61.0 + starlette<1.4.0 조합.
# starlette는 범위로 두어 streamlit이 요구하는 호환 버전으로 해석되게 함(1.4.0만 차단).
streamlit==1.61.0
starlette<1.4.0
prophet==1.3.0
cmdstanpy==1.3.0
pandas==3.0.5
numpy==2.5.1
matplotlib==3.11.1
plotly==6.9.0
korean-lunar-calendar==0.4.0
requests==2.34.2
ephem==4.2.1
